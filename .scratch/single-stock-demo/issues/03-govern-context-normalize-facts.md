# 03 — 治理上下文并生成规整事实

**What to build:** 让用户可以从冻结研究快照得到围绕主研究问题的可定位上下文，再把其中的候选数值和声明转换为保留原值、公式与会计轨迹的规整事实。该 ticket 完成 `govern-research-context` 与 `normalize-research-facts`，坚持先缩小上下文、后做技术和会计规整。

**Blocked by:** 02 — 已完成；Snapshot v2 构建后，两份经用户确认的 transcript 通过新身份晋升为当前 Snapshot v3。

**Status:** done

- [x] 上下文片段均有 `chunk_id`，可回溯 `material_id`，并保留页码、章节、表格/数据行、文本哈希及数字所需的表头、单位、期间和脚注；`context.jsonl` 保存全部合格片段并独立记录 `candidate_context`、命中查询和选择原因，规整阶段只处理候选上下文。
- [x] 权威片段采用结构原子而非固定长度重叠窗口：PDF 正文按章节内段落组、表格按携带表头/单位/期间/脚注的表或行组、HTML 按标题栏目、transcript 按带所属栏目的 speaker turn 切分；命中可关联相邻片段但不生成新 chunk，`chunk_id` 不受查询和排名变化影响。
- [x] 只处理 Snapshot v3 `smic-a283e95e2c9e8068` 中可解析、与 Acquisition Target 相关的 `ACQUIRED_UNASSESSED` 材料；上下文治理只负责相关性、分块、Content Locator 和 BM25，不判断真实性、信源等级、内容类型或证据资格。
- [x] 关键词/BM25 是可删除、可重建的本地基线，固定查询记录命中片段、定位正确性和已知漏召回；检索分数不被当作信源可信度或证据强度。
- [x] 主研究问题和 D1-D7 使用版本化固定 Query Family；每族包含显式中英文子查询，各子查询独立 BM25 评分后取命中并集，并保存原始查询、分词结果、排名和分数；禁止运行时模型扩写或改写查询。
- [x] 查询族分为 `RESEARCH` 与 `NORMALIZATION_GUARD`：前者覆盖主问题和 D1-D7，后者固定覆盖会计范围、期间单位和调整桥接；两类均可选择候选上下文但分别验收，护栏命中不能冒充研究支持。
- [x] BM25 使用仓库内固定版本的确定性中英混合分词器：索引侧 NFKC/大小写/标点规整，中文领域词表最长匹配加未匹配二元字组，英文单词/数字及复合词，固定领域符号和季度 token，并记录分词器、词表、停用词版本及实际查询 tokens；不得改写原文或动态扩词。
- [x] 每个子查询以固定锚点和正 BM25 分数准入，按 `score desc, chunk_id asc` 取全局 Top 20、单材料最多 5 个直接命中，第 20 名同分全收但仍受单材料上限；不设跨查询绝对分数阈值，参数修改必须由固定漏召回验收支持并更新规则版本。
- [x] 候选过滤只排除并显式记录结构噪声，不使用来源、内容类型、可信度或观点方向；`AI Summary` 保留栏目身份并可被召回；直接命中作同材料同栏目前后各一片段扩展，表格不扩展，所有选择和过滤原因可审计。
- [x] RAG 验收使用与 v3 和查询规则版本绑定的 locator 级固定标注集，区分 `MUST_HIT`、`SHOULD_HIT`、`NEGATIVE_CONTROL`；标注只判断相关性，不判断真实性或证据资格，快照/规则变化必须显式换版。
- [x] 子查询各自 Top 20 后按 `chunk_id` 合并为 Family 且不二次截断；只有合格 `DIRECT_HIT` 计入召回。`MUST_HIT Recall=100%`，`SHOULD_HIT Recall` 总体不低于 80% 且任一有标注 Family 不低于 60%；每子查询 `Precision@min(5,N)>=60%` 且 `N=0` 计 0；负控候选率为 0，关键定位/数字解释要素完整率为 100%，两次干净重建哈希一致。
- [x] 独立输出只表示检索质量的 `retrieval_status=PASS/WARN/FAIL`：PASS 可规整且满足 RAG 验收；WARN 可规整但必须登记 gap 且 ticket 不自动通过；FAIL 按可界定的查询族局部阻断，否则全局阻断。该状态不得表达事实、信源、证据或报告资格。
- [x] BM25 PASS 时不做 Embedding，FAIL 时先修复确定性基线；仅 WARN 且预登记词法漏召回时允许一次同集实验。实验分 `ADOPTED`（WARN 转 PASS）、`IMPROVED_NOT_ADOPTED`（有预登记召回改善但仍 WARN）和 `REJECTED`；只有 ADOPTED 可进入 Demo，且不建设向量数据库、长期知识库、同步服务、重排服务或检索 API。
- [x] 每项规整事实分别保存主体、指标、原始与规整数值、单位、币种、缩放、期间/时点、报告类型、会计准则、合并范围、审计状态、来源定位和转换轨迹。
- [x] `normalized-facts.jsonl` 一行一个原子记录，`record_kind` 仅为 `NUMERIC_OBSERVATION`、`TEXT_PROPOSITION` 或 `DERIVATION`；多指标/多期间/数值与归因拆行，重复披露分别保留，派生值新建记录并引用输入，所有最终 `claim_type` 仍为 `UNASSESSED`。
- [x] 文本命题保留完整原文跨度、说话人/发布主体、栏目、时点和目标期间；拆分必须保留否定、条件、不确定性及适用范围，共同限定条件随每项保存，不能无损拆分时保持一条，且不得把定性或条件陈述改成确定数值/事实。
- [x] 数值同时保留原文和 Decimal 字符串，单位/币种/缩放分离，百分比与百分点分开，括号负数可追溯且不增加显示精度；`value_status` 明确区分存在、显式零、不适用、未报告、不可解析和 UNKNOWN，空白/破折号/N/A 不自动转零。
- [x] 产能、出货、利用率和 ASP 保留晶圆口径及测量基准；不同实体/等值晶圆、月产能/期末/季度平均不得自动换算或比较，利用率不反推，来源 ASP 与满足全口径一致条件的隐含 ASP 派生分别保存，不建设通用单位转换器。
- [x] 制程/应用结构保留原始分类、分母及体系版本，不同分母不得混用，仅按固定映射归组；分类合计只在计算前已确认同分母、互斥且完整的集合执行，不能由接近 100% 反推资格，不合格时 `CHECK_NOT_APPLICABLE`，分类变化标记 `TAXONOMY_CHANGE`。
- [x] 已发生资本开支、购建资产现金流支出、承诺金额、计划/指引和项目投资使用不同指标并分别保留；规整阶段不互相替代，也不推断资本开支已经转化为产能、收入或利润。
- [x] 货币金额精确缩放为原币 `base_unit_value`，但持续携带原表精度且不得解释为更精确的真实值；非整除或需舍入的转换必须作为独立派生/转换保存完整 Decimal、公式和舍入轨迹，无获批舍入规则时为 UNKNOWN/TBD。
- [x] 指标和主体仅通过版本化固定映射表及不改变语义的确定性标签规整映射，保留原始标签、映射状态、规则 ID、版本和哈希；禁止模糊/语义归并。映射与可比性分离，可比状态必须绑定比较基准或输入事实并记录固定断裂原因。
- [x] 期间以解析状态、性质、类型和准确日期边界表达；未知/歧义用状态、null 和 gap，不参与比较。单季度与累计至季度字段分离，月份数可选、天数确定性计算；披露 TTM 是原始观察，四个可比单季度构成的 TTM 是独立派生记录，两者不覆盖。
- [x] 累计相减仅在同起点 YTD 输入的指标、主体、准则、范围、币种、单位、审计版本和定义均可比且差额构成固定案例财政单季度时执行；输出独立派生记录，输入舍入影响使用三态枚举，失败保留阻断原因和 gap，不建设通用财政日历引擎。
- [x] 系统派生只允许版本化固定白名单函数，禁止公式 DSL/运行时自由公式；非正基期或符号变化不生成相对变化率；TTM 只由四个连续、互不重叠、全口径可比的单季度组成；新增公式必须显式换版并补齐 fixtures。
- [x] 主分析口径为 IFRS、合并报表和 USD；CAS 只作调节，币种转换、派生指标、累计相减和 TTM 均保留合格输入与可复算公式，不满足可比条件时输出 UNKNOWN 或 `COMPARABILITY_BREAK`。
- [x] IFRS/CAS 只实现固定 `CAS_TO_IFRS` 桥接：明确 BASE/ADJUSTMENT_DETAIL/SUBTOTAL/TARGET 行角色，只汇总明细，并保留来源符号、ADD/SUBTRACT 和规范带符号金额；指标定义、权益归属、主体范围、准则角色、币种单位、版本重述及流量/时点期间必须匹配。
- [x] IFRS/CAS 复算差异按来源披露精度确定固定容差；零差异 PASS、容差内舍入差异 WARN、超差异为局部核验 FAIL，精度不足且非零时 UNKNOWN。核验仅附着为目标事实的 `reconciliation_check`；局部 FAIL/公式不匹配只使桥接 BLOCKED、运行至少 WARN，不直接成为规整运行 FAIL，所有来源原子事实仍保留；不建设通用对账引擎。
- [x] `value_origin` 与 `currency_conversion_status` 分离；系统换汇只在冻结汇率可用时新建引用原事实和汇率的派生记录。v3 无独立汇率材料，不运行时补汇率；无转换的 CNY/USD 不得跨币种运算，CAS 只作调节。
- [x] 会计准则、币种、合并范围和审计状态逐字段独立解析并各自保留来源，不打包继承；审计状态只覆盖审计意见明确适用的财务报表及附注，年报中的经营 KPI/管理层内容不自动视为已审计，且不建设通用继承框架。
- [ ] 同期间同口径的重述关系只依据 `as_of` 前发行人的明确说明建立；原值与各次重述值全部保留，最新明确重述仅作为会计比较优先版本而不提前获得证据资格。差异但未明确重述时标记 `VERSION_CONFLICT` 和 gap，不自动选值，也不建设通用版本图。
- [x] 经常性经营盈利能力按确认的指标组处理，所有规整结果展示“报告值 -> 调整项 -> 规整值”桥接；政府补助同时呈现报告与剔除敏感性口径，折旧和研发不被自动剔除。
- [x] 政府资金剔除只生成并列敏感性，不覆盖报告 IFRS 主口径；只有可分离、可追溯且确认计入同口径营业利润的金额可调整，不以全部其他营业收入或 CAS 扣非表替代，并展示完整 fact 桥接。
- [x] 可调整政府资金只含当期已确认进基础营业利润的可分离金额，排除现金收款、资本投入、资产补助/递延收益余额、税项和重复调整；初始调整白名单仅此一项，其他候选只保留事实/命题及固定原因的 `ADJUSTMENT_TBD`，不建设动态审批框架。
- [x] 年度、累计期、单季度、TTM、期间数和时点数不会混用；百分点与百分比变化得到区分，不做机械年化或无来源转换。
- [x] 缺失单位、期间、币种、范围或调整依据不会被默认值和模型常识补齐；相关 UNKNOWN/TBD 均进入唯一 gap 登记册。
- [x] 规整产物的最终 `claim_type` 保持 `UNASSESSED`；如保存内容类型提示，必须明确为非权威提示，最终类型和证据资格留给 Ticket 04。
- [x] 记录级 `normalization_status` 固定为 PASS/PARTIAL/BLOCKED：PARTIAL 表示来源可安全保留但部分规整属性未知，BLOCKED 表示具体操作不能安全产出且只保留输入、原因和 gap。运行级固定按 `FAIL > WARN > PASS` 汇总；只有定位/哈希/契约/ID/轨迹/确定性等完整性错误为 FAIL，存在目标 PARTIAL/BLOCKED 或规整 gap 为 WARN。
- [x] 正常、缺字段、公式不可复算、口径断裂和 RAG 漏召回 fixtures 能验证两个 Skills 的输入、输出和降级行为。

## Comments

- 2026-07-20：用户确认 `context.jsonl` 保存全部可检索片段，候选上下文作为独立选择状态；该结构用于复现召回并显式验收已知漏召回，`normalize-research-facts` 只读取候选上下文。
- 2026-07-20：用户确认原 Candidate Holding 中两份 transcript 的发布时间并生成自包含 Snapshot v3 `smic-a283e95e2c9e8068`。离线 `DEMO_RUN` 通过；v3 的 20 份材料均为 `ACQUIRED_UNASSESSED`，两份 transcript 已冻结并解析。v3 中四份 SIA HTML 的原始哈希相对 v2 变化，但解析后的标题和正文一致，属于网页外壳变化；Ticket 03 以 v3 为输入。
- 2026-07-20：用户确认权威 chunk 采用可复现的结构原子，不采用固定 token 长度的重叠窗口；检索时允许关联已有相邻片段，但不得据此制造新的权威 chunk。
- 2026-07-20：用户确认固定查询采用 Query Family，而不是每个维度一条长自然语言查询；每族使用显式、版本化的中英文子查询，分别执行 BM25 并合并结果，运行时不做模型查询扩写。
- 2026-07-20：用户确认查询职责分为 `RESEARCH` 和 `NORMALIZATION_GUARD`；研究查询覆盖主问题和 D1-D7，规整护栏查询固定覆盖 `G_ACCOUNTING_SCOPE`、`G_PERIOD_UNIT`、`G_ADJUSTMENT_BRIDGE`，两类命中分别验收。
- 2026-07-20：用户确认 BM25 使用仓库内固定版本的确定性中英混合分词规则：索引侧文本规整、中文领域词最长匹配与二元字组回退、英文及领域符号保留、季度写法规范 token、固定停用词和完整分词版本记录；同义表达仅由固定子查询提供。
- 2026-07-20：用户确认候选截断采用固定词法锚点、正 BM25 分数、每子查询全局 Top 20 和单材料最多 5 条；边界同分全收，排序稳定，不使用跨查询绝对分数阈值。
- 2026-07-20：用户确认候选过滤仅基于结构噪声且保留过滤命中记录；`AI Summary` 不提前排除或定性；直接命中只在同材料同栏目做前后各一个现有片段的邻接扩展，自包含表格不扩展。
- 2026-07-20：用户确认 RAG 使用 locator 级固定验收标注集，按 `MUST_HIT`、`SHOULD_HIT`、`NEGATIVE_CONTROL` 管理，并与快照及查询规则版本绑定；标注只用于召回相关性验收。
- 2026-07-20：用户确认 RAG 量化验收定义：各子查询先独立 Top 20，Family 按 `chunk_id` 取并集且不二次截断；只有合格直接命中计召回，多查询命中保留全部查询 ID；Precision 按子查询 `min(5,N)` 计算且 `N=0` 为 0。关键召回、负控、定位、数字上下文和确定性采用硬指标，术语使用“RAG 验收门槛”而非发布门禁。
- 2026-07-20：用户确认 `retrieval_status` 只评价 RAG 检索质量，不代表事实正确、证据充分或报告资格；全局硬失败阻断全部规整，能可靠界定到查询族的局部失败只阻断受影响范围，无法界定时按全局失败处理。
- 2026-07-20：用户确认可选 Embedding/混合检索实验分为 `ADOPTED`、`IMPROVED_NOT_ADOPTED`、`REJECTED`；部分词法召回改善可留作研究记录，但只有把 BM25 WARN 转为 PASS 且硬指标不退化时才允许进入 Demo。
- 2026-07-20：用户确认规整事实采用原子粒度：每行只表达一个数值观察、文本命题或派生记录；多指标、多期间及数值与归因必须拆行，重复披露不在规整阶段合并，派生值不得覆盖报告值，结构类型不替代 Ticket 04 的最终内容分类。
- 2026-07-20：用户确认数值同时保存原文与 Decimal 定点字符串，单位/币种/缩放分离，百分比和百分点不得混用；括号负数保留原始显示，空白/破折号/N/A 不自动解释为零，并使用明确的值状态和 UNKNOWN/gap 降级。
- 2026-07-20：用户确认指标/主体仅通过确定性规整后的固定别名与定义映射，保留原始标签、规则、版本和哈希，禁止模糊或模型语义归并；映射成功不代表可比，可比性必须绑定明确比较基准或输入事实，并保存状态与原因码。
- 2026-07-20：用户确认期间模型使用 `period_mapping_status` 与条件期间字段，区分单季度和累计至季度，准确起止日期为主、月份数可选、天数按首尾包含计算；直接披露 TTM 与系统四季度派生 TTM 分别保存且不得覆盖。
- 2026-07-20：用户确认货币缩放使用 `base_unit_value` 且不制造底层金额精确到基础单位的假象，原表精度必须随记录保留；非整除转换使用 Decimal 并保存舍入前后、模式和位置，未确认舍入规则时不得隐式出值。
- 2026-07-20：用户确认 `value_origin` 与 `currency_conversion_status` 正交；来源披露值均为 SOURCE_REPORTED，系统换汇新建 SYSTEM_DERIVED/COMPLETED 记录。v3 无合格汇率时相关转换为 UNAVAILABLE，不同币种不得直接运算，CAS 仍只作调节。
- 2026-07-20：用户确认“黄金案例优先与架构克制”：只实现当前 v3 黄金案例和硬验收所需能力，不为假设鲁棒性或未来复用增加架构；不影响硬正确性和可追溯性的局限优先记录为限制或 gap，调整能力来自直接代码和显式规则而非扩展框架。
- 2026-07-20：用户确认会计准则、币种、合并范围和审计状态必须逐字段独立解析、继承和降级；审计状态只适用于明确覆盖范围，已审计年报中的管理层讨论、经营 KPI 和补充数据不自动获得 AUDITED，采用固定字段规则而非通用继承引擎。
- 2026-07-20：用户确认累计相减仅在全部口径一致且按中芯国际固定自然年度边界构成完整单季度时执行，不建设通用财政日历；输入精度影响使用 ROUNDED/UNROUNDED/UNKNOWN，派生失败保留阻断状态、原因和 gap。
- 2026-07-20：用户确认系统派生使用当前案例固定函数白名单且不建设公式 DSL；相对变化遇到零/负基期或符号变化时阻断，TTM 只使用四个连续不重叠可比单季度，新增公式必须预先入白名单、分配规则 ID、换版并补齐 fixtures。
- 2026-07-20：用户确认报告 IFRS 营业利润/率保留为主口径，政府资金剔除仅为并列敏感性；调整金额必须可分离、可追溯并确认计入同口径营业利润，不得以全部其他营业收入或 CAS 非经常性项目代替，折旧和研发不参与该调整。
- 2026-07-20：用户确认可调整政府资金严格限于当期已确认进入基础营业利润的可分离金额，排除现金、资本投入、资产/递延余额、税项和重复剔除；输出只称敏感性。初始调整白名单仅此一项，其他候选用固定原因码的 ADJUSTMENT_TBD 记录，不建设动态调整/审批架构。
- 2026-07-20：用户确认 IFRS/CAS 桥接采用最小固定实现：方向仅 `CAS_TO_IFRS`，显式区分基础值、调整明细、小计和目标并只汇总明细；指标定义、权益归属、主体范围、期间/时点、币种单位及版本必须匹配。复算按披露精度判定零差异、舍入差异、公式不匹配或 UNKNOWN，结果只作为目标事实的核验元数据；失败不删除来源事实，也不建设通用对账框架。
- 2026-07-20：用户确认重述与版本使用固定最小规则：仅依据发行人在 `as_of` 前明确说明建立重述关系；原披露和全部重述版本均保留，最新明确重述只作为会计比较优先值，不代表证据资格。仅有数值差异时不得推断替代关系，而应标记 `VERSION_CONFLICT` 和 gap；不建设通用版本图。
- 2026-07-20：用户确认技术指标使用黄金案例固定规则：产能/出货保留实体或等值晶圆口径及时间基准，利用率不反推，来源与隐含 ASP 分开；制程/应用结构保留原分类和分母，仅对事前确认完整、互斥、同分母的集合核验合计；资本开支实际、现金流、承诺、指引和项目投资不混用，也不在规整阶段推断因果。
- 2026-07-20：用户确认文本命题拆分必须保留完整原文跨度以及否定、条件、不确定性和适用范围，无法无损拆分时保持整条；规整状态使用记录级 PASS/PARTIAL/BLOCKED 和运行级 PASS/WARN/FAIL，PARTIAL 与 BLOCKED 职责分开并按 `FAIL > WARN > PASS` 确定性汇总。
- 2026-07-21：额度中断后复核实际实现进度。对着 `smic-a283e95e2c9e8068` 端到端跑通 `scripts/govern_research_context.py`（`retrieval_status: PASS`）和 `scripts/normalize_research_facts.py`（生成 20114 行 `normalized-facts.jsonl`，`normalization_run_status: WARN`，符合预期——PARTIAL 记录已登记 gap）；`pytest tests/test_govern_research_context_cli.py tests/test_normalize_research_facts_cli.py` 27 passed。据此把已被代码和测试证实覆盖的验收项勾选为完成。仍未实现、需要补做的 4 项：
  1. 制程/应用结构的分类合计校验（`CHECK_NOT_APPLICABLE`/`TAXONOMY_CHANGE`）——目前只保留了 `composition_set_id`/`composition_denominator_id`，没有真正做同分母互斥完整性校验或合计核验。
  2. 资本开支类指标（实际/现金流/承诺/指引/项目投资）是否已用不同 `metric_id` 严格分开——未在代码中找到显式区分逻辑，需要用 fixture 补充验证或在 `rules/accounting.yaml` 补齐映射。
  3. 重述/版本冲突处理——`restatement_status` 目前硬编码为 `ORIGINAL`，没有检测发行人明确重述声明或标记 `VERSION_CONFLICT` 的逻辑。
  4. 政府资金调整白名单之外候选的 `ADJUSTMENT_TBD` 记录——代码里没有生成该状态，白名单外的候选目前没有被显式登记。
  另外，Embedding 实验（`ADOPTED`/`IMPROVED_NOT_ADOPTED`/`REJECTED`）本次未触发也未实现，因为当前 `retrieval_status` 已是 PASS，按规则不需要该实验；暂标记为不适用，不阻塞本 ticket。
- 2026-07-21：对上一条 4 项缺口做了排查与收尾（走 A：把 ticket 做扎实再进 04）。以真实 v3 候选事实（20114 条）为依据判定与实现：
  1. 制程/应用分类合计（#1）：**已实现**。对 `APPLICATION_REVENUE_SHARE`/`WAFER_SIZE_REVENUE_SHARE` 事实新增 `composition_check`；v3 无任何“事前确认同分母、互斥且完整”的分类集合，故固定输出 `CHECK_NOT_APPLICABLE`（reason `SET_NOT_CONFIRMED_COMPLETE_EXCLUSIVE_SAME_DENOMINATOR`），绝不由接近 100% 反推资格。`TAXONOMY_CHANGE` 作为保留状态：v3 每个集合只有单一固定 taxonomy 版本，无跨期分类变化可触发，故不建跨期 taxonomy diff（架构克制）。新增 fixture `test_composition_share_records_not_applicable_total_check`。
  2. 资本开支区分（#2）：**核实后确认已满足**。5 类 capex（实际/现金流/承诺/指引/项目投资）在 `rules/accounting.yaml` 已各自映射到不同 `metric_id`，规整阶段无任何跨 capex 派生或替代；v3 候选中实际只出现 `CAPITAL_EXPENDITURE_INCURRED`（22 条）。新增 fixture `test_capex_metrics_stay_distinct_without_substitution` 锁定“不同指标、不合并、不替代”。
  3. 重述/`VERSION_CONFLICT`（#3）：**登记为限制，不实现检测**。v3 的 20 份材料中不存在“发行人在 `as_of` 前明确声明、同期间同口径原值与重述值并存”的场景；出现的“revised/restated/retrospective”均为 CSRC 非经常性损益规则修订、公司章程修订、或新准则追溯性*采用*，非财务数值重述。跨报表出现的数值差异全部来自表格解析（本期/上期列、同比列共享一个 span 被归到同一期间）的口径噪声——若据此建 naive 数值差检测会大量误报 `VERSION_CONFLICT`，反而破坏正确性。故按“黄金案例优先 + 架构克制”保持 `restatement_status=ORIGINAL`，不建通用版本图；该路径为未触发路径，待真出现明确重述材料时再实现。
  4. 政府资金 `ADJUSTMENT_TBD`（#4）：**已实现**。新增 `tag_government_funding_candidates`：只有真正喂入确认敏感性的“可分离、已确认进营业利润”金额记 `ADJUSTMENT_CONFIRMED`；其余 `GOVERNMENT_FUNDING_RECOGNIZED_IN_PNL` 候选全部保留并标 `adjustment_candidate_status=ADJUSTMENT_TBD` + 固定原因码 `NOT_PROVEN_IN_BASE_METRIC`（取自 `rules.adjustment_tbd_reason_codes`），不丢弃、不静默调整、不建审批框架。v3 中 15 条政府资金候选全部落入 TBD。新增 fixture `test_non_whitelist_government_funding_is_adjustment_tbd`。
  收尾验证：`retrieval_status=PASS`、`normalization_run_status=WARN`（PARTIAL 均登记 gap）、两次干净重建哈希一致、全量 `pytest tests/` 50 passed。
