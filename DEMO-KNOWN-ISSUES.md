# Demo 已知问题清单

**目的**：记录 Demo 构建过程中发现的问题，不在当前阶段处理。先跑通端到端，再回头逐条评估。
**日期**：2026-07-21
**当前进度**：Ticket 01–04 done，05/06 待做
**处置原则**：不解决可以，不记录不行。本清单不构成待办，只构成"已知"。

---

## A. 已确认的问题

### A1. 事实抽取存在语义错误，且不影响证据状态

**现象**：抽样 `tmp/ticket03-final/normalized-facts.jsonl` 发现 `FACT_ecbb3d82b20ae58fed788265`：

```
原文  息税折旧摊销前利润率（%） 53.8 52.4 增加1.4个百分点
      （EBITDA 利润率 52.4% → 53.8%，提高 1.4 个百分点）
抽成  metric_id = DEPRECIATION_EXPENSE，1400 元人民币，FY2025
      normalization_status = PASS → T1 / REPORTED_FACT / USABLE / 可承重
```

**规模**：`raw_unit=MONEY` 且原文含"百分点"的记录，4,575 条数字事实中有 **179 条（3.9%）**。
⚠️ 这是启发式粗筛，非精确统计，未人工核对真实错误率。

**三个独立成因**：

| # | 缺陷 | 代码位置 |
|---|---|---|
| 1 | 单位正则 `(?P<unit>%\|percentage points?\|pp)?` 只认英文，中文"个百分点"不匹配 → fallback 到 chunk 级 `numeric_context.units`（"单位：千元 币种：人民币"）→ 判成 MONEY ×1000 | `normalize_research_facts.py:26`、`:763`、`numeric_context_defaults():683` |
| 2 | 指标别名 `折旧` 用 `re.finditer(re.escape(...))` 纯子串扫描，命中"息税**折旧**摊销前利润率"内部；候选排序取 `match.start()` 最小者，位置靠前的"折旧"胜出 | `metric_mapping():397`、`accounting.yaml:113` |
| 3 | 列表头兜底未生效（详见 B1） | `metric_for_numeric_position():361` |

**为什么没被拦住**：三档证据状态（USABLE/RESTRICTED/QUARANTINED）的六条判定条件，全部在判"元数据健不健全"（时间、定位、准则、单位期间、信源等级、冲突），**没有一条在判"抽取的语义对不对"**。整条链路上也没有任何一关做过「值」与「原文」的一致性比对。

**以后怎么解决**（从便宜到贵）：
1. **加交叉校验门禁**（约 1 天）：`raw_unit == MONEY` 但 `source_span_text` 含"百分点" → 判矛盾，降 `PARTIAL` → 证据降 `RESTRICTED` 不可承重。治标，但能一次拦住这 179 条
2. **修抽取层**（约 2–3 天）：中文别名加词边界检查（左右紧邻汉字则判 `UNMAPPED`）+ 正则补 `个百分点|百分点`。治本一半，但会改变几千条记录的 `metric_id`，须重跑 03/04，fixture 可能大面积要改
3. **LLM 抽取 + 构建期冻结**（1 周+）：见 D1

---

### A2. `QUARANTINED` 隔离机制从未被真实数据触发

**现象**：`tmp/ticket04-final/governed-evidence.jsonl` 共 20,114 条：

| 状态 | 条数 | 占比 |
|---|---|---|
| `USABLE` | 15,202 | 75.6% |
| `RESTRICTED` | 4,912 | 24.4% |
| `QUARANTINED` | **0** | 0% |

且 RESTRICTED 数量与规整阶段 `PARTIAL` 数量**完全相等**（都是 4,912），说明另外两条受限理由（`LOW_SOURCE_TIER`、`CONFLICT_UNRESOLVED`）实际上没有独立触发过。

**意味着**：三条隔离规则（`MATERIAL_AS_OF_INELIGIBLE`、`CONTENT_LOCATOR_UNRESOLVABLE`、`NORMALIZATION_BLOCKED`）**只有 fixture 测试验证过，真实数据上一次都没跑到**。

**两种解读，目前无法区分**：
- 采集阶段确实把不合格材料挡在门外了（好事）
- 隔离机制存在但从未被真实检验（风险）

**以后怎么解决**：故意构造一份越界材料走一遍全流程，确认隔离真的会触发、且下游真的引不到。或者接受"fixture 验过即可"，但要显式记录这个取舍。

---

### A3. 事实到证据是 1:1，证据治理未过滤任何记录

**现象**：`normalized-facts.jsonl` 20,114 条 → `governed-evidence.jsonl` 20,114 条。**一条没少**。

**意味着**：04 的作用目前是"给每条事实贴标签"，而不是"筛掉不合格的"。所有事实都进了证据集，只是分成 USABLE/RESTRICTED 两档。

这本身**符合设计**（项目一贯是"保留一切、只限制用途"），但值得记下来：**证据治理没有起到"收窄"的作用**，收窄的责任全压在 05 挑证据那一步。

**以后怎么解决**：等 05 做完再看。如果 05 从 15,202 条 USABLE 里挑证据挑得很痛苦，说明 04 应该承担更多筛选。

---

### A4. 冲突检测范围被缩到只剩 2 条白名单事实

**现象**：`ADR 0003` 把冲突检测限制在人工核对过的 `fact_id` 白名单上，白名单目前只有 2 条用户确认的 FY2024 利润事实。

**意味着**：`CONFLICT_UNRESOLVED` 这条受限理由在 20,114 条里几乎不可能触发（数据也印证了——RESTRICTED 全部由 PARTIAL 触发）。**"多来源冲突检测"这个能力在真实数据上等于没开。**

**这是一个有意识的取舍**（为了"Demo 简约"、避免全量冲突检测的复杂度），不是 bug。但如果报告里想展示"我们能发现来源冲突"，目前展示不出来。

**以后怎么解决**：等 06 生成报告后看。如果报告里冲突章节是空的、显得这个能力不存在，再决定要不要扩白名单（比如扩到 D1 用到的核心指标）。

---

## B. 待验证的怀疑

### B1. `column_headers` 键名可能不匹配，导致表头兜底静默失效

**代码**（`metric_for_numeric_position():361`）：

```python
column_headers = (chunk.get("numeric_context") or {}).get("column_headers") or []
if column_index is None or column_index >= len(column_headers):
    return metric          # 放弃兜底，用句子猜的结果
```

**观察**：抽样看到的 chunk，`numeric_context` 的键是 `{footnotes, headers, periods, units}`——叫 `headers`，**不叫 `column_headers`**，且值为空 `[]`。

**如果属实**：`column_headers` 恒为 `[]`，条件恒成立，**这层兜底逻辑从来没有执行过**。不报错、不留痕、fixture 测试大概率用的是代码期望的键名所以照样通过。

⚠️ **只在一个 chunk 上看到过，未做全量统计。**

**怎么验**（一行）：
```python
import json, collections
c = collections.Counter()
for l in open('tmp/ticket03-final/context.jsonl', encoding='utf-8'):
    c.update((json.loads(l).get('numeric_context') or {}).keys())
print(c)
```
- 键不存在 → 结构本来就没提取（归因到 B2）
- 键叫 `headers` 而代码读 `column_headers` → **单纯的字段名笔误**，好修
- 键存在但大量为空 → 提取了没填上

---

### B2. PDF 表格结构可能在解析阶段就被压平

**两条旁证**：

1. `source_span_text` 是一根空格连接的字符串：`"息税折旧摊销前利润率（%） 53.8 52.4 增加1.4个百分点"`。原文在 PDF 里几乎肯定是**四个独立单元格**（指标名 / 本期 / 上年同期 / 变动）
2. `table_column_index(sentence, position)` 的签名是"**从句子和字符位置推列号**"。如果上游交付的是真正的单元格网格，这个函数根本不该存在——它的存在本身说明下游只能靠数分隔符倒推列

**如果属实，影响面很大**：
- A1 的成因 1 升级——"1.4"本在独立单元格里，根本不该受表头"千元"约束
- B1 的成因也归此——没有单元格就没有列表头可给
- **Ticket 01/02 的验收条件"PDF 正文、表格、页码、章节、脚注及定位得到保留"当时可能没有被真正验证**（测试大概率只验了"有没有产出表格对象"，没验"单元格切得对不对"）

**怎么验**：
打开 `single-stock-demo-v3/parsed/MATERIAL_SMIC_2025_H1_A_SHARE_REPORT_CNINFO.json`，搜 "息税折旧" 或 "53.8"：
- `{"cells": [["息税折旧摊销前利润率（%）","53.8","52.4","增加1.4个百分点"]]}` → 结构还在，是 chunk 阶段丢的（Ticket 03 的问题）
- `{"text": "息税折旧摊销前利润率（%） 53.8 ..."}` → **解析阶段就丢了**（Ticket 01/02 的欠债）

同时对照原始 PDF `single-stock-demo-v3/raw/...pdf` 第 7 页第 3 张表，确认原文排版是否规整（跨页？合并单元格？无框线？）——这决定"保住结构"的难度和收益。

**⚠️ 这条的验证优先级最高**：它的结论决定 A1 该修哪一层，也决定 D1 的方案是否成立。

---

## C. 观察到，但不确定是不是问题

### C1. Ticket 04 在真实数据上是 WARN 不是 PASS

`tmp/ticket04-final/evidence-validation.yaml` 记录了具体是哪几项警告。**本次未展开看**。

推 05 之前值得扫一眼——如果警告项和 A1 有关，那 05 的证据池质量就有已知折扣。

### C2. 事实数是候选片段数的 6 倍

3,399 个候选 chunk → 20,114 条事实，平均每片段抽出约 6 条。其中 `TEXT_PROPOSITION` 占 77%（15,539 条），`NUMERIC_OBSERVATION` 只占 23%（4,575 条）。

不确定这个比例是否合理。文字命题这么多，05 挑证据时可能面临大量低信息量记录。等 05 做完再评估。

### C3. 仓库里权威产物和历史残留的边界不清

现存：`single-stock-demo/`（v1）、`single-stock-demo-v2/`、`single-stock-demo-v2-candidate-holding/`、`single-stock-demo-v3/`、`tmp/ticket03-baseline/`、`tmp/ticket03-final/`、`tmp/ticket03-normalized/`、`tmp/ticket04-final/`、`tmp/pdfs/`。

v1/v2 按设计是"不可变历史结果"要保留，但 `tmp/` 下几个中间目录哪些还有用、哪些是残留，目前没有说明。**06 要输出"最终产物清单"时会撞上这个问题。**

---

## D. 已经想过但明确推迟的方案

### D1. LLM 抽取 + 构建期冻结

**思路**：复用项目已有的 `SNAPSHOT_BUILD` / `DEMO_RUN` 分离，把 LLM 放进构建期，输出冻结成带哈希的产物。

```
构建期（跑一次，可慢、可不确定）
  chunk → LLM 抽取 → extracted-facts.jsonl + 内容哈希 → 冻结
运行期（每次跑，必须确定、可复现）
  extracted-facts.jsonl → 脚本校验+换算+留痕 → normalized-facts.jsonl
```

**分工**：LLM 负责"读懂"（什么指标、什么单位、哪个期间、是表格值还是附注差值），脚本负责"算对 + 记账 + 卡关"（Decimal 精度、换算留痕、桥接复算、三档判定）。

**必须配套**（缺一不可）：
1. **原文回填**：LLM 须输出依据的原文片段，脚本做精确子串匹配，对不上判 `BLOCKED`
2. **交叉一致性校验照写**：A1 那条门禁与抽取器无关，是安全网。LLM 也会错，只是错得不一样
3. **抽取标注集**：仿检索阶段的 `Retrieval Acceptance Set`，人工标 100–200 条，换抽取方式必须证明准确率不降

**明确不做**：运行时调 LLM（破坏可复现，违反 spec "禁止运行时模型扩写或改写查询"的同源精神）；LLM 直接产出最终 `normalized-facts.jsonl`（换算必须确定性可验）；LLM 参与三档判定（那是纯规则）。

**推迟理由**：
- 若 B2 属实（解析层就丢了结构），LLM 拿到的同样是压平字符串，**这是在坏地基上盖房子**
- 05 还没做，不知道下游真正需要什么质量的事实，现在动手是在猜
- 可能违背"Demo 简约"（引入新产物、新构建步骤、新标注集），需要 grill

### D2. 缩小承重范围到白名单指标

承重证据其实只需覆盖 D1–D7 真正用到的指标。可以只保证一个**白名单指标集**（毛利率、收入、资本开支、利用率等）的抽取正确性，其余一律降 RESTRICTED。

思路和 ADR 0003 把冲突检测缩到白名单**完全一致**，符合"Demo 简约"。等 05 做完、知道到底用了哪些指标之后再评估。

---

## E. 推 05/06 时的低成本缓解

**不改任何逻辑，只改报告展示**：让报告在展示承重证据时，把 `source_span_text`（原文片段）和抽取结果**并排印出来**。

```
D1 判定 SUPPORTED
承重证据 EV-xxx：折旧费用 1400 元（FY2025，CAS，合并口径）
原文：息税折旧摊销前利润率（%） 53.8 52.4 增加1.4个百分点
```

抽取错误会自己跳出来，不用加门禁、不用改抽取逻辑。

这本来也是"可核验"的应有之义：**可核验 = 人能核，不是系统保证对**。系统的责任是把核验所需的东西摆到人面前。

---

## F. 下次回来先做什么

按成本从低到高：

1. **验 B2**（10 分钟）—— 结论决定其他所有事怎么修
2. **验 B1**（1 分钟）—— 一行脚本
3. **人工抽 20 条 A1 的记录**（半小时）—— 确定真实错误率
4. 然后再决定 A1 走方案 1 / 2 / D1